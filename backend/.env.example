# Shared secret with the CORE gateway (required; no fallback - the service
# fails closed without it). Generate a long random value.
INTERNAL_SERVICE_TOKEN=
# Optional; defaults to sqlite:///./am_pricing.db
# DATABASE_URL=
